<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.damien.campusordering.mapper.SetmealMapper">
    <!--    根据分类id查询套餐-->
    <select id="list" parameterType="com.damien.campusordering.entity.Setmeal"
            resultType="com.damien.campusordering.entity.Setmeal">
        select * from setmeal
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="getDishItemBySetmealId" resultType="com.damien.campusordering.vo.DishItemVO">
        select sd.name, sd.copies, d.image, d.description
        from setmeal_dish sd
                 left join dish d on sd.dish_id = d.id
        where sd.setmeal_id = #{setmealId}
    </select>
</mapper>
