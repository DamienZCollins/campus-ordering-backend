<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.damien.campusordering.mapper.CategoryMapper">
    <select id="pageQuery" resultType="com.damien.campusordering.entity.Category">
        SELECT *
        FROM category
        <where>
            <if test="name != null and name != ''">
                AND NAME LIKE concat('%', #{name}, '%')
            </if>
            <if test="type != null">
                AND TYPE = #{type}
            </if>
        </where>
        ORDER BY sort ASC, create_time DESC
    </select>
</mapper>
