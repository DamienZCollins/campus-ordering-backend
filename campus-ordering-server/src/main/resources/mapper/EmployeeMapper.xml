<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.damien.campusordering.mapper.EmployeeMapper">
    <select id="pageQuery" resultType="com.damien.campusordering.entity.Employee">
        SELECT * FROM employee
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <update id="update" parameterType="com.damien.campusordering.entity.Employee">
        UPDATE employee
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="name != null">name = #{name},</if>
            <if test="password != null">password = #{password},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="idNumber != null">id_number = #{idNumber},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="getById" resultType="com.damien.campusordering.entity.Employee">
        SELECT id,
               username,
               name,
               password,
               phone,
               sex,
               id_number,
               status,
               create_time,
               create_user,
               update_time,
               update_user
        FROM employee
        WHERE id = #{id}

    </select>

</mapper>

